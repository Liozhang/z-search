/**
 * Multi-Provider Configuration Types
 *
 * Core types for the Feature-based provider selection system
 */

/** AI feature module identifiers */
export type AIFeature =
  "translation" | "chat" | "agent" | "vision" | "embedding";
